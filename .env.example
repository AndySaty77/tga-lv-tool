# Basis-URL der App (z. B. für Passwort-Reset-E-Mail-Link). Ohne Angabe: lokal localhost:3000, sonst window.origin.
# NEXT_PUBLIC_SITE_URL=https://ihre-domain.de

# Supabase (für Triggers + Scoring-Config)
NEXT_PUBLIC_SUPABASE_URL=https://dein-projekt.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=dein_anon_key
# Server-only (zwingend für gehärtete Write-/Admin-Pfade, nicht NEXT_PUBLIC_)
# SUPABASE_SERVICE_ROLE_KEY=dein_service_role_key

# OpenAI (server-only, für LLM-Split, Nachtragsanalyse, Vortext, etc.)
OPENAI_API_KEY=sk-...

# Optional: anderes Modell (Standard: gpt-4o-mini)
# OPENAI_MODEL=gpt-4o-mini

# Admin-Bereich (/admin, /api/admin/*): nur diese E-Mail-Adressen haben Zugriff (kommasepariert)
# ADMIN_EMAILS=admin@example.com,team@example.com

# Kontaktformular (server-only) – E-Mail-Versand mit Resend
RESEND_API_KEY=re_...
CONTACT_FORM_TO_EMAIL=empfaenger@example.com
# Optional: eigener Absender (sonst: onboarding@resend.dev für Tests)
# CONTACT_FORM_FROM_EMAIL=TGA LV Tool <kontakt@ihredomain.de>

# Öffentlichen Stripe-Checkout für Pro deaktivieren (Einladungs-/Anfragemodus). UI blendet Kaufbuttons aus.
# DISABLE_PUBLIC_PRO_CHECKOUT=true
# Wenn gesperrt: nur diese E-Mails dürfen trotzdem POST /api/billing/checkout (kommasepariert, trim/lowercase). Leer = keine Ausnahme.
# STRIPE_CHECKOUT_ALLOWED_EMAILS=max@example.com,anna@example.com

# Stripe (nur Server): Pro-Abo Checkout + Webhook – siehe app/api/billing/*
# STRIPE_SECRET_KEY=sk_...
# STRIPE_WEBHOOK_SECRET=whsec_...
# STRIPE_PRICE_PRO_MONTHLY=price_...
# Webhook-URL in Stripe: https://<ihre-domain>/api/billing/webhook
# Customer Portal im Stripe Dashboard aktivieren (Billing → Customer portal), sonst schlägt /api/billing/portal fehl.
# Optional: Basis-URL für Checkout-Redirects (sonst NEXT_PUBLIC_SITE_URL / VERCEL_URL)
# NEXT_PUBLIC_APP_URL=https://ihre-domain.de
